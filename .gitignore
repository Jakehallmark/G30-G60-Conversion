# Runtime conversion output
Converted/

# Customer / site relay settings — never commit G30 sources or converted outputs
*.xml
!bases/G60 Base*.xml
*.urs
!bases/G60 Base*.urs

# Local UR firmware images (encrypted binaries; keep Analogoperand CSVs)
firmware/**/*.SFD
firmware/**/*.sfd
firmware/**/*.000
firmware/**/*.VRC
firmware/**/*.vrc
_tmp_signal_probe/
_tmp_firmware_list.txt

# Conversion reports and other HTML output
*.html

# Local test fixtures and dev scripts
test_*.py
test_out*.txt
**/validate_bases.py
HCHPublix*.xml
Leesburg*.xml
Publix*.xml

# Stress-test run output (keep _tmp_stress/run_stress.py)
_tmp_stress/runs/
_tmp_stress/runs_*/

# Unused NuGet spec (release workflow publishes the exe directly)
packaging/

# PyInstaller / pack output
build/
dist/
aio/build/
aio/dist/
aio/dist-test/
aio/pack-staging/
aio/*.spec

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.python_packages/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
/.vscode/
.vscode/

# Optional local docs assets (PDF briefs, etc.) — keep committed validation report
docs/*
!docs/STRESS_TEST_REPORT.md

# Prebuilt GUI installer (too large for git; see GitHub Releases)
release/*.exe
release/RELEASE_NOTES.md
